using System;
using System.Collections.Generic;
using System.Linq;
using System.ServiceModel;
using System.Text;
using System.Threading.Tasks;

namespace MarketMiner.Common.Faults
{
   public class FaultExceptionFactory
   {
      //public static FaultException<NotFoundFault> NewNotFoundFaultException(string message, string code = null, string action = null)
      //{
      //   var faultReason = new FaultReason(message);

      //   if (code == null) code = "UnspecifiedFault";
      //   var faultCode = new FaultCode(code);

      //   if (action == null) action = "Contact your administrator.";

      //   return new FaultException<NotFoundFault>(faultReason, faultCode, action);
      //}
   }
}
